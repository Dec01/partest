# What ships to PyPI. Distribution is PyPI-only (docs/wiki/decisions/pypi-only.md),
# so anything listed here is public forever.
include partest/py.typed
recursive-include partest/docs *.md
include LICENSE
include CHANGELOG.md

# Deliberately NOT shipped:
#   tests/      — reference a consumer project by name, and several of them need
#                 repo-only files (tools/, docs/wiki/, .claude/) so they cannot pass
#                 from an sdist anyway
#   README.md   — repo-facing; it links to docs/wiki/, AGENTS.md and .claude/skills/,
#                 none of which ship. The PyPI description comes from docs/PYPI.md
#   docs/       — the wiki, raw consumer snapshots and the archive are internal
prune tests
prune docs
prune src
prune tools
prune .claude
prune .grok
exclude README.md
exclude conftest.py
exclude confpartest.py
exclude setup_project.py
exclude pytest.ini
exclude Dockerfile
exclude requirements.txt
